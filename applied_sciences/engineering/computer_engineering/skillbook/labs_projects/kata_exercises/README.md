Languages: implement a hash map, linked list, parser for a tiny grammar, write a fast CSV reader, implement a thread-safe queue   
   
Algorithms: binary search variants, top-K, string matching, DP classics   
   
Systems: write a simple allocator, implement strace-like syscall logger, parse ELF headers, write a toy linker step   
   
Tools: “kata” for Git (rebase + fixup + bisect), Docker (build multi-stage image), Kubernetes (write a manifest that passes probes), Terraform (module + state move)   
